# pnpm config for the revival. strict-peer-dependencies relaxed during the
# migration; tighten once the NestJS 11 / TypeORM peer set is settled.
strict-peer-dependencies=false
auto-install-peers=true
